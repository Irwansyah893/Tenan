package com.example.ui.theme

import androidx.compose.ui.graphics.Color

// Warm Palette (Warung Theme with Orange, Cream, and Brown accents)

// Sleek Theme colors (Light)
val PrimaryOrange = Color(0xFFF27D26)    // Bold warm orange primary brand color
val PrimaryOrangeLight = Color(0xFFFFF1EB) // Subtle light orange background highlights (10% opacity equivalent)
val SecondaryBrown = Color(0xFF5D4037)   // Elegant dark coffee brown (Primary brand body text)
val TertiaryCream = Color(0xFF8D6E63)     // Light warm slate/brown highlight color
val SleekCreamBg = Color(0xFFFCF8F1)      // Warm cream organic background color (#FCF8F1)
val SleekCreamSurface = Color(0xFFFFFFFF) // High contrast clean crisp white card surfaces
val BorderCreamAccent = Color(0xFFEADDCD) // Delicate cream outline border color (#EADDCD)

// Sleek Theme colors (Dark)
val PrimaryOrangeDim = Color(0xFFFA9052)  // Luminous responsive sleek orange
val SecondaryBrownDim = Color(0xFFD7CCC8) // Brightened slate coffee/cream text
val TertiaryCreamDim = Color(0xFFBCAAA4)  // Warm gray brown Accent
val EspressoBg = Color(0xFF1E1815)        // Ultra rich roasted warm dark coffee espresso background
val EspressoSurface = Color(0xFF2B201A)   // Cozy dark warm espresso surface cards
val TextLightCream = Color(0xFFFCF8F1)    // Cozy text color companion for dark theme
val BorderDarkAccent = Color(0xFF4D362B)  // Dark contrast border accent
